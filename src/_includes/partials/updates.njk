<ul>
  {% for post in collections.updates | limit(5) %}
  <li>{% set definedDate = post.date %} {% include "partials/date.njk" %}: {{ post.data.title }}</li>
  {% endfor %}
</ul>

<p class="text-center" style="font-size: var(--size-step-0);"><a href="/updates/">All site updates</a></p>