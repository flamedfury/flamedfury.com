<!DOCTYPE html>
<html lang="en">
{%- set pageTitle %}{% if title %}{{ title }} : {% endif %}{{ meta.siteName }} {{ meta.siteVer }}{% endset -%}
{%- set pageDescription %}{% if description %}{{ description }}{% else %}{{ meta.siteDescription }}{% endif %}{% endset -%}
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <meta name="description" content="{{ pageDescription }}">

    <title>{{ pageTitle }}</title>

    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="shortcut icon" href="/img/favicon.png" />
</head>

<body>

    <header class="header">
      <img src="../../img/header.png" width="1200" height="400" alt="welcome to fLaMEdFury!">
    </header>

    <div class="wrapper">

      <nav class="navigation">
          {% include "partials/_nav.njk" %}
      </nav>

      <main class="content">
        <h1>{{ title }}</h1>
        {{ content | safe }}
      </main>

      <aside class="sidebar">
        {% include "partials/_sidebar.njk" %}
      </aside>

      <footer class="t-center">
        {% include "partials/_footer.njk" %}
      </footer>

    </div>

    <script  src="../js/main.js"></script>

  </body>
</html>
